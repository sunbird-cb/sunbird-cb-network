package org.sunbird.cb.hubservices.serviceimpl;

import org.junit.runner.RunWith;
import org.mockito.junit.MockitoJUnitRunner;

@RunWith(MockitoJUnitRunner.class)
class ProfileServiceTest {

	/*
	 * @InjectMocks ProfileService profileService;
	 * 
	 * @Mock ConnectionService connectionService;
	 * 
	 * @Mock ConnectionProperties connectionProperties;
	 * 
	 * final static String MOCK_UUID = "user_id";
	 * 
	 * @BeforeEach public void setUp(){ MockitoAnnotations.initMocks(this); }
	 * 
	 * 
	 * @Test void findCommonProfile() {
	 * 
	 * List<UserConnection> mockConList = new ArrayList<>();
	 * 
	 * Response mockresponse = new Response();
	 * mockresponse.put(Constants.ResponseStatus.DATA, mockConList);
	 * when(connectionService.findSuggestedConnections(MOCK_UUID, 0,
	 * 1)).thenReturn(mockresponse);
	 * 
	 * Response res = profileService.findCommonProfile(MOCK_UUID, 0, 1);
	 * assertTrue(((List)res.getResult().get(Constants.ResponseStatus.DATA)).size()=
	 * =0); }
	 * 
	 * @Test void findProfileRequested() {
	 * 
	 * List<UserConnection> mockConList = new ArrayList<>();
	 * 
	 * Response mockresponse = new Response();
	 * mockresponse.put(Constants.ResponseStatus.DATA, mockConList);
	 * when(connectionService.findConnectionsRequested(MOCK_UUID, 0,
	 * 1)).thenReturn(mockresponse);
	 * 
	 * Response res = profileService.findProfileRequested(MOCK_UUID, 0, 1);
	 * assertTrue(((List)res.getResult().get(Constants.ResponseStatus.DATA)).size()=
	 * =0); }
	 */

}